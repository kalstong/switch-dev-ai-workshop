# switch-dev-ai-workshop

## Project overview

Workshop project for Switch Dev AI. This is a new project — see below for conventions as they are established.

## Development

<!-- Update these sections as the project takes shape -->

### Setup

```bash
# TODO: Add setup instructions once the tech stack is chosen
```

### Commands

```bash
# TODO: Add common commands (build, test, lint, dev server, etc.)
```

## Conventions

- Keep code simple and readable
- Write tests for new functionality
- Use conventional commit messages (e.g., `feat:`, `fix:`, `docs:`, `chore:`)
